# STAY DIMANSION 브랜드 디자인 규칙

이 레포에서 STAY DIMANSION 브랜드 소재(하우스룰, 안내문, 홍보물, 웰컴카드 등)를 만들거나 수정할 때 반드시 지킬 규칙입니다. `/design`으로 캔버스를 만들 때도 동일하게 적용합니다.

브랜드에 국한되지 않는 일반 그래픽/편집 디자인 원칙(대비·반복·정렬·근접성·위계·균형, 화이트스페이스, 그리드)은 `.claude/skills/editorial-design/SKILL.md` 참고 — 이 문서는 그 원칙을 STAY DIMANSION에 맞게 구체적인 숫자·서체로 확정한 버전이다.

## 타이포그래피

자세한 서체·CDN 링크는 [typography.md](./typography.md) 참고. 요약:

- 영문 워드마크(STAY DIMANSION): **Cormorant Garamond**
- 국문 헤드라인: **Maru Buri (마루 부리)**
- 본문: Pretendard(또는 typography.md의 대안 3종: SUIT / Spoqa Han Sans Neo / Wanted Sans)

## 표기 규칙

- **이탤릭체 금지 — 영문·국문 모두.** `font-style: italic`을 어디에도 쓰지 않는다. 한글 폰트는 진짜 이탤릭 글리프가 거의 없어서 브라우저가 억지로 기울이면(synthetic oblique) 획이 뭉개져 지저분해 보인다. 영문도 통일성을 위해 함께 금지.
- **제목을 제외하고는 어디도 강조하지 않는다.** 이게 제일 자주 어기는 규칙이라 구체적으로 적는다 — 아래 전부 금지:
  - `font-weight: 700`(볼드)을 제목/헤드라인/워드마크가 아닌 곳에 쓰는 것 (예: 데이터 값을 굵게 — ID/PW, 객실번호, 와이파이 비밀번호 등)
  - 본문 문장 중 특정 구절만 accent 컬러 등으로 다르게 칠하는 것 (`<span style="color:{{accent}}">...`)
  - 특정 항목에만 배경색 강조 박스를 씌우는 것 (예: 규칙 목록 중 하나만 하이라이트 배경)
  - 위 세 가지 모두 "다른 항목보다 더 중요해 보이게" 만드는 시도라서 금지. 정말 중요한 안내라도 같은 목록 안 다른 항목과 완전히 동일한 스타일을 쓴다.
  - **라벨(ID/PW 같은 작은 무채색 캡션) 옆에 오는 값은 라벨과 완전히 같은 크기, 볼드 없이 쓴다.** 처음엔 "본문 기본 굵기(600)까지는 괜찮다"고 했었는데, 실제로 보니 그것도 강조로 읽혀서 기각됨 — 와이파이 ID/PW 값을 라벨(`font-size:10.5px`)과 동일한 크기·굵기(굵기 지정 없음, 기본값)로 수정한 게 최종 기준. 값이 라벨보다 커 보이거나 굵어 보이면 그 자체가 규칙 위반.
- **부가 설명·번역문처럼 "덜 중요함"을 표시할 때**는 이탤릭 대신 **작은 크기 + 무채색(muted) 컬러** 조합을 쓴다 (House Rules의 영문 번역 줄 참고).
- **한글 자간은 살짝 좁힌다**: `.kr` 클래스에 `letter-spacing: -0.02em` 적용(Noto Sans KR 기본 자간이 좀 넓어 보여서). 영문 캡션류는 각자 지정한 `letter-spacing`(대개 양수, 트래킹)을 그대로 유지 — 인라인 스타일이 클래스보다 우선하므로 안 깨짐.
- **한글 줄바꿈은 항상 `word-break: keep-all;`을 쓴다** (`overflow-wrap: break-word;`를 안전장치로 같이). 이게 없으면 "루프탑에서 드시(줄바꿈)고, 쓰레기는"처럼 조사·어미가 단어 중간에서 잘려 다음 줄로 넘어가는 사고가 난다. 이 속성은 어절(단어+조사/어미) 단위로만 줄바꿈을 허용해서 이런 문제를 막는다.
- **본문 텍스트 크기는 생각보다 작게.** House Rules는 14.5px로 시작해서 "크다"는 피드백을 두 번 받고 12px까지 줄임. 새 본문을 쓸 때 처음부터 13px 이하로 시작하고, 필요하면 그때 키운다(반대로 하지 않는다).

## 정렬 · 여백

간격은 소재 성격에 따라 두 스케일 중 하나를 골라 쓴다(둘을 섞지 않는다 — 한 소재 안에서는 하나만).

**A. 정보 밀도형 (하우스룰, 안내문처럼 항목이 많은 문서)** — House Rules에 적용된 8px 그리드:
  - `8px` — 캡션/번역문과 본문 사이
  - `12px` — 아이콘·숫자뱃지와 옆 텍스트, 같은 그룹 안 요소
  - `16px` — 항목과 항목 사이
  - `24px` — 섹션 내부 큰 블록 간
  - `32px` — 메인 헤드라인 아래 (최소값, 이보다 좁히지 않는다)
  - `48px` — 섹션과 섹션 사이

**B. 여백형 (웰컴카드, 포스터, SNS처럼 정보가 적고 여백 자체가 브랜드 느낌을 주는 소재)** — 1.6배수 스케일:
  - `10px` — 인라인 그룹(아이콘+텍스트)
  - `16px` — 항목 간격
  - `26px` — 소제목 아래
  - `42px` — 메인 헤드라인 아래
  - `68px` — 큰 섹션 구획 / 포스터 상단 여백

- **레이아웃은 항상 flex(행/열) + `gap`.** 형제 요소 사이 여백은 `margin`이 아니라 부모의 `gap`으로 준다(다시 배치·삭제해도 여백이 안 깨지도록).
- **메인 헤드라인 아래 여백은 위 스케일의 최솟값이지 목표값이 아니다.** 처음 House Rules는 22px였다가 "너무 오밀조밀하다"는 피드백으로 32px(A스케일)까지 늘림 — 촘촘함보다 여유가 이 브랜드의 기본값. 고정 크기 인쇄물이라 공간이 빠듯하면, 헤드라인 여백을 줄이지 말고 본문 분량(문구·항목 수)을 줄인다.
- 새로 만들 소재가 A/B 어느 쪽에 가까운지 애매하면 정보량으로 판단한다: 항목이 6개 이상이거나 표/목록이 있으면 A, 그 외(카드 한 장, 한 줄 메시지 위주)는 B.
- **섹션 제목**은 소제목 텍스트 + `gap:10px` + 옅은 밑줄(`border-bottom:1.5px solid`, 무채색 라인) + `padding-bottom:7px` 조합으로 통일한다.
- **박스형 정보 카드**(와이파이 안내처럼 배경이 있는 박스)는 내부 여백 `padding:13px`, 모서리 `border-radius:6px`. 더 작은 key-value 줄은 `padding:7px 10px`, `border-radius:5px`.
- **페이지 바깥 여백**은 위쪽을 아래쪽보다 살짝 넉넉하게 준다(House Rules는 상 44px / 하 32px, 좌우 56px).
- **정렬 기준**: 아이콘+텍스트 한 줄은 `align-items:flex-start`, 섹션 제목처럼 베이스라인을 맞출 땐 `align-items:baseline`, 하단 서명/푸터처럼 중앙 정렬할 땐 `align-items:center`.

## 색상

`Stay Dimansion House Rules` 캔버스 기준 accent 계열(`#c1633c` 등)을 따른다. 새 accent 색을 임의로 만들지 말고 기존 팔레트에서 고른다.
